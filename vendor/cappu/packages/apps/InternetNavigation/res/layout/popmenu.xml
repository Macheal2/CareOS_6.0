<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <TextView android:id="@+id/apptitle" style="@style/option_dialog_title" android:text="@string/bookmark_option"/>

    <RelativeLayout
        android:id="@+id/delBookMark"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dialog_margin"
        android:layout_marginLeft="@dimen/dialog_margin"
        android:layout_marginRight="@dimen/dialog_margin"
        android:layout_marginTop="14dp"
        android:background="@drawable/dialog_bottom_selector"
        android:clickable="true"
        android:gravity="center_vertical" >

        <Button
            android:clickable="false"
            android:textAllCaps="false"
            android:id="@+id/btnDelBookMark"
            style="@style/option_dialog_button_only"
            android:text="@string/bookmark_del" />

    </RelativeLayout>

</LinearLayout>